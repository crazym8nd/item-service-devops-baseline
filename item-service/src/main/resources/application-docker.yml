spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:item_service}

  jpa:
    show-sql: false

  flyway:
    enabled: true

  sql:
    init:
      mode: never
